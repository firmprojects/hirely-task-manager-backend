{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "skipBuild": true
      }
    }
  ],
  "env": {
    "PRISMA_CLIENT_ENGINE_TYPE": "dataproxy"
  },
  "buildCommand": "prisma generate && next build || exit 0"
}
